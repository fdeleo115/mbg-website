{
  "name": "mbg-website",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --port=${PORT:-3000}",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "wrangler": "^3.99.0"
  }
}
